Created an Express.js server that serves user data from a JSON file. It includes routes for listing all users, retrieving a specific user by ID, and providing a REST API for CRUD operations i.e. adding, updating, and deleting users. Data is persisted by writing updates back to the JSON file. It also includes middleware for parsing URL-encoded request bodies and listens for incoming requests on port 8000.
